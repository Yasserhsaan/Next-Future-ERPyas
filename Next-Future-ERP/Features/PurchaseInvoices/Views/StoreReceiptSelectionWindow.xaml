<Window x:Class="Next_Future_ERP.Features.PurchaseInvoices.Views.StoreReceiptSelectionWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="اختيار سند الاستلام" Height="600" Width="1000"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResize">
    
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- العنوان -->
        <TextBlock Grid.Row="0" Text="اختيار سند الاستلام" 
                   FontSize="18" FontWeight="Bold" 
                   HorizontalAlignment="Center" Margin="0,0,0,10"/>

        <!-- شريط البحث -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock Text="البحث:" VerticalAlignment="Center" Margin="0,0,5,0"/>
            <TextBox Name="SearchTextBox" Width="200" Height="25" 
                     TextChanged="SearchTextBox_TextChanged"/>
            <Button Name="RefreshButton" Content="تحديث" Width="80" Height="25" 
                    Margin="5,0,0,0" Click="RefreshButton_Click"/>
        </StackPanel>

        <!-- جدول سندات الاستلام -->
        <DataGrid Grid.Row="2" Name="StoreReceiptsGrid" 
                  ItemsSource="{Binding}"
                  AutoGenerateColumns="False" 
                  CanUserAddRows="False" 
                  CanUserDeleteRows="False"
                  SelectionMode="Single"
                  SelectionChanged="StoreReceiptsGrid_SelectionChanged">
            <DataGrid.Columns>
                <DataGridTextColumn Header="رقم السند" Binding="{Binding ReceiptNumber}" Width="120"/>
                <DataGridTextColumn Header="التاريخ" Binding="{Binding ReceiptDate}" Width="100"/>
                <DataGridTextColumn Header="المورد" Binding="{Binding SupplierName}" Width="200"/>
                <DataGridTextColumn Header="المبلغ الإجمالي" Binding="{Binding TotalAmount}" Width="120"/>
                <DataGridTextColumn Header="الحالة" Binding="{Binding StatusText}" Width="100"/>
                <DataGridTextColumn Header="الوصف" Binding="{Binding Description}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- أزرار التحكم -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" 
                    HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Name="SelectButton" Content="اختيار" Width="80" Height="30" 
                    Margin="0,0,10,0" Click="SelectButton_Click" IsEnabled="False"/>
            <Button Name="CancelButton" Content="إلغاء" Width="80" Height="30" 
                    Click="CancelButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
