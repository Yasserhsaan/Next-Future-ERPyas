<UserControl x:Class="Next_Future_ERP.Features.StoreIssues.Views.StoreIssuesView"
              xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
              xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
              xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
              xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
              xmlns:core="clr-namespace:Next_Future_ERP.Core.Controls"
              xmlns:converters="clr-namespace:Next_Future_ERP.Features.StoreIssues.Converters"
              mc:Ignorable="d"
              FlowDirection="RightToLeft"
              Background="#F7F9FC">

    <UserControl.Resources>
        <!-- Common Converters -->
        <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
        
        <!-- Styles -->
        <Style TargetType="Button" x:Key="ToolbarBtn">
            <Setter Property="Padding" Value="10,6"/>
            <Setter Property="Margin" Value="6,0,0,0"/>
        </Style>
        <Style TargetType="DataGrid">
            <Setter Property="RowHeight" Value="36"/>
            <Setter Property="AlternationCount" Value="2"/>
            <Setter Property="AlternatingRowBackground" Value="#FAFAFA"/>
            <Setter Property="CanUserAddRows" Value="False"/>
            <Setter Property="IsReadOnly" Value="True"/>
            <Setter Property="SelectionMode" Value="Single"/>
        </Style>
    </UserControl.Resources>

    <DockPanel Margin="12">
        <!-- شريط أدوات -->
        <Border DockPanel.Dock="Top" Background="#F3F4F6" CornerRadius="8" Padding="10" Margin="0,0,0,10">
            <DockPanel LastChildFill="False">
                <core:CommandBar DockPanel.Dock="Top"
                                 NewCommand="{Binding AddNewCommand}"
                                 EditCommand="{Binding EditSelectedCommand}"
                                 SaveCommand="{x:Null}"
                                 DeleteCommand="{Binding DeleteSelectedAsyncCommand}"
                                 RefreshCommand="{Binding LoadAsyncCommand}"
                                 SearchCommand="{Binding SearchCommand}" />
                <StackPanel Orientation="Horizontal" Margin="10,0,0,0">
                    <Button Content="📤 ترحيل" 
                            Command="{Binding PostSelectedAsyncCommand}" 
                            Style="{StaticResource ToolbarBtn}"
                            IsEnabled="{Binding CanPost}"/>
                    <Button Content="❌ إلغاء" 
                            Command="{Binding CancelSelectedAsyncCommand}" 
                            Style="{StaticResource ToolbarBtn}"
                            IsEnabled="{Binding CanCancel}"/>
                </StackPanel>
            </DockPanel>
        </Border>

        <!-- القائمة -->
        <DataGrid ItemsSource="{Binding Issues}"
                  SelectedItem="{Binding SelectedIssue, Mode=TwoWay}"
                  AutoGenerateColumns="False"
                  MouseDoubleClick="DataGrid_MouseDoubleClick">
            <DataGrid.Columns>
                <DataGridTextColumn Header="رقم المستند" 
                                    Binding="{Binding IssueNumber}" 
                                    Width="150"/>
                
                <DataGridTextColumn Header="التاريخ" 
                                    Binding="{Binding IssueDate, StringFormat={}{0:yyyy-MM-dd}}" 
                                    Width="120"/>
                
                <DataGridTextColumn Header="جهة الصرف" 
                                    Binding="{Binding DestinationName}" 
                                    Width="200"/>
                
                <DataGridTextColumn Header="المخزن الافتراضي" 
                                    Binding="{Binding DefaultWarehouseName}" 
                                    Width="150"/>
                
                <DataGridTextColumn Header="المبلغ الإجمالي" 
                                    Binding="{Binding TotalAmount, StringFormat={}{0:N2}}" 
                                    Width="120"/>
                
                <DataGridTextColumn Header="العملة" 
                                    Binding="{Binding CurrencyName}" 
                                    Width="100"/>
                
                <DataGridTextColumn Header="الحالة" 
                                    Binding="{Binding StatusText}" 
                                    Width="100">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="Foreground" Value="{Binding StatusColor}"/>
                            <Setter Property="FontWeight" Value="SemiBold"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                
                <DataGridTextColumn Header="الوصف" 
                                    Binding="{Binding Description}" 
                                    Width="*"/>
            </DataGrid.Columns>
            
            <DataGrid.ContextMenu>
                <ContextMenu>
                    <MenuItem Header="تعديل" Command="{Binding DataContext.EditSelectedCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"/>
                    <MenuItem Header="ترحيل" Command="{Binding DataContext.PostSelectedAsyncCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"/>
                    <MenuItem Header="إلغاء" Command="{Binding DataContext.CancelSelectedAsyncCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"/>
                    <Separator/>
                    <MenuItem Header="حذف" Command="{Binding DataContext.DeleteSelectedAsyncCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"/>
                </ContextMenu>
            </DataGrid.ContextMenu>
        </DataGrid>
    </DockPanel>
</UserControl>
