<Window x:Class="Next_Future_ERP.Features.Inventory.Views.InventoryOpeningBrowseView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="استعراض مستندات الرصيد الافتتاحي" Height="520" Width="860"
        WindowStartupLocation="CenterOwner" FlowDirection="RightToLeft">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="المعروضة: مسودات فقط (غير المعتمدة)" Foreground="#6B7280" Margin="0,0,0,8"/>

        <DataGrid Grid.Row="1" ItemsSource="{Binding Headers}" SelectedItem="{Binding SelectedHeader}"
                  AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="م" Width="60" Binding="{Binding DocID}"/>
                <DataGridTextColumn Header="رقم المستند" Width="150" Binding="{Binding DocNo}"/>
                <DataGridTextColumn Header="التاريخ" Width="120" Binding="{Binding DocDate, StringFormat=yyyy/MM/dd}"/>
                <DataGridTextColumn Header="الحالة" Width="120" Binding="{Binding StatusText}"/>
                <DataGridTextColumn Header="ملاحظات" Width="*" Binding="{Binding Notes}"/>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Left" Margin="0,10,0,0">
            <Button Content="اختيار" Width="100">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="IsEnabled" Value="True"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding SelectedHeader.Status}" Value="Approved">
                                <Setter Property="IsEnabled" Value="False"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>
            <Button Content="إغلاق" Width="100" Margin="8,0,0,0" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>


