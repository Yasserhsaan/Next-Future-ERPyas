<Page
    x:Class="Next_Future_ERP.Features.Accounts.Views.PaymentVoucherListView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    xmlns:controls="clr-namespace:Wpf.Ui.Controls;assembly=Wpf.Ui"
    xmlns:core="clr-namespace:Next_Future_ERP.Core.Controls"
    Title="سندات الصرف"
    FlowDirection="RightToLeft"
    Background="#F7F9FC">

    <Page.Resources>
        <Style TargetType="Button" x:Key="ToolbarBtn">
            <Setter Property="Padding" Value="10,6"/>
            <Setter Property="Margin" Value="6,0,0,0"/>
        </Style>
        <Style TargetType="DataGrid">
            <Setter Property="RowHeight" Value="36"/>
            <Setter Property="AlternationCount" Value="2"/>
            <Setter Property="AlternatingRowBackground" Value="#FAFAFA"/>
            <Setter Property="CanUserAddRows" Value="False"/>
            <Setter Property="IsReadOnly" Value="True"/>
            <Setter Property="SelectionMode" Value="Single"/>
        </Style>
    </Page.Resources>

    <!-- نجعل كل العناصر تتراكم للأعلى -->
    <DockPanel Margin="12" LastChildFill="False">

        <!-- شريط الأدوات أعلى الصفحة -->
        <controls:Card DockPanel.Dock="Top" Padding="10" Margin="0,0,0,10">
            <core:CommandBar DataContext="{Binding}"
                             NewCommand="{Binding NewDialogCommand}"
                             EditCommand="{Binding EditDialogCommand}"
                             SaveCommand="{x:Null}"
                             DeleteCommand="{Binding DeleteCommand}"
                             RefreshCommand="{Binding RefreshCommand}"
                             SearchCommand="{Binding LoadCommand}" />
        </controls:Card>

        <!-- القائمة (الداتا جريد) مباشرة أسفل الشريط ومتراصة للأعلى) -->
        <controls:Card DockPanel.Dock="Top"
                       Padding="0"
                       VerticalContentAlignment="Top"
                       HorizontalContentAlignment="Stretch">
            <DataGrid ItemsSource="{Binding Items}"
                      SelectedItem="{Binding SelectedItem, Mode=TwoWay}"
                      AutoGenerateColumns="False"
                      MouseDoubleClick="DataGrid_MouseDoubleClick"
                      VerticalAlignment="Top"
                      ScrollViewer.VerticalScrollBarVisibility="Auto">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="الرقم" Binding="{Binding VoucherID}" Width="80"/>
                    <DataGridTextColumn Header="رقم المستند" Binding="{Binding DocumentNumber}" Width="120"/>
                    <DataGridTextColumn Header="التاريخ" Binding="{Binding DocumentDate, StringFormat=dd/MM/yyyy}" Width="100"/>
                    <DataGridTextColumn Header="الفرع" Binding="{Binding BranchName}" Width="*"/>
                    <DataGridTextColumn Header="النوع" Binding="{Binding VoucherType}" Width="90"/>
                    <DataGridTextColumn Header="المصدر" Binding="{Binding SourceName}" Width="*"/>
                    <DataGridTextColumn Header="المستفيد" Binding="{Binding Beneficiary}" Width="*"/>
                    <DataGridTextColumn Header="المبلغ المحلي" Binding="{Binding LocalAmount, StringFormat=N3}" Width="130">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="TextAlignment" Value="Right"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>
                </DataGrid.Columns>

                <DataGrid.ContextMenu>
                    <ContextMenu>
                        <MenuItem Header="تعديل"
                                  Command="{Binding DataContext.EditDialogCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"/>
                        <MenuItem Header="حذف"
                                  Command="{Binding DataContext.DeleteCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"/>
                    </ContextMenu>
                </DataGrid.ContextMenu>
            </DataGrid>
        </controls:Card>

    </DockPanel>
</Page>

