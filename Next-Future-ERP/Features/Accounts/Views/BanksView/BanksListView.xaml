<Page
    x:Class="Next_Future_ERP.Features.Accounts.Views.BanksListView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    xmlns:controls="clr-namespace:Wpf.Ui.Controls;assembly=Wpf.Ui"
    xmlns:core="clr-namespace:Next_Future_ERP.Core.Controls"
    Title="البنوك"
    FlowDirection="RightToLeft"
    Background="#F7F9FC">

    <Page.Resources>
        <Style TargetType="Button" x:Key="ToolbarBtn">
            <Setter Property="Padding" Value="10,6"/>
            <Setter Property="Margin" Value="6,0,0,0"/>
        </Style>
        <Style TargetType="DataGrid">
            <Setter Property="RowHeight" Value="36"/>
            <Setter Property="AlternationCount" Value="2"/>
            <Setter Property="AlternatingRowBackground" Value="#FAFAFA"/>
            <Setter Property="CanUserAddRows" Value="False"/>
            <Setter Property="IsReadOnly" Value="True"/>
            <Setter Property="SelectionMode" Value="Single"/>
        </Style>
    </Page.Resources>

    <!-- تكديس العناصر من الأعلى -->
    <DockPanel Margin="12" LastChildFill="False">

        <!-- شريط الأدوات -->
        <controls:Card DockPanel.Dock="Top" Padding="10" Margin="0,0,0,10">
            <core:CommandBar DataContext="{Binding}"
                             NewCommand="{Binding NewDialogCommand}"
                             EditCommand="{Binding EditDialogCommand}"
                             SaveCommand="{x:Null}"
                             DeleteCommand="{Binding DeleteCommand}"
                             RefreshCommand="{Binding RefreshCommand}"
                             SearchCommand="{Binding LoadCommand}" />
        </controls:Card>

        <!-- القائمة (الداتا جريد) أسفل الشريط مباشرة وملاصقة للأعلى -->
        <controls:Card DockPanel.Dock="Top"
                       Padding="0"
                       VerticalContentAlignment="Top"
                       HorizontalContentAlignment="Stretch">
            <DataGrid ItemsSource="{Binding Items}"
                      SelectedItem="{Binding SelectedItem, Mode=TwoWay}"
                      AutoGenerateColumns="False"
                      MouseDoubleClick="DataGrid_MouseDoubleClick"
                      VerticalAlignment="Top"
                      ScrollViewer.VerticalScrollBarVisibility="Auto">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="الرقم" Binding="{Binding BankId}" Width="80"/>
                    <DataGridTextColumn Header="اسم البنك" Binding="{Binding BankName}" Width="*"/>
                    <DataGridTextColumn Header="الحساب" Binding="{Binding AccountNumber}" Width="120"/>
                    <DataGridTextColumn Header="الشركة" Binding="{Binding Company.CompName}" Width="*"/>
                    <DataGridTextColumn Header="الفرع" Binding="{Binding Branch.BranchName}" Width="*"/>
                    <DataGridCheckBoxColumn Header="نشط" Binding="{Binding IsActive}" Width="80"/>
                </DataGrid.Columns>

                <DataGrid.ContextMenu>
                    <ContextMenu>
                        <MenuItem Header="تعديل"
                                  Command="{Binding DataContext.EditDialogCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"/>
                        <MenuItem Header="حذف"
                                  Command="{Binding DataContext.DeleteCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"/>
                    </ContextMenu>
                </DataGrid.ContextMenu>
            </DataGrid>
        </controls:Card>

    </DockPanel>
</Page>
