<Window x:Class="Next_Future_ERP.Features.Accounts.Views.FundEditWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        xmlns:controls="clr-namespace:Wpf.Ui.Controls;assembly=Wpf.Ui"
        xmlns:models="clr-namespace:Next_Future_ERP.Models"
        xmlns:sys="clr-namespace:System;assembly=System.Runtime"
        Title="{Binding WindowTitle}" Height="700" Width="900"
        WindowStartupLocation="CenterOwner"
        FlowDirection="RightToLeft"
        Background="#F7F9FC"
        ResizeMode="CanResizeWithGrip"
        MinWidth="800" MinHeight="600"
        Loaded="Window_Loaded">

    <Window.Resources>
        <ObjectDataProvider x:Key="FundFunctionValues"
                            MethodName="GetValues"
                            ObjectType="{x:Type sys:Enum}">
            <ObjectDataProvider.MethodParameters>
                <x:Type TypeName="models:FundType"/>
            </ObjectDataProvider.MethodParameters>
        </ObjectDataProvider>

        <!-- ألوان -->
        <SolidColorBrush x:Key="CardBg" Color="#FFFFFFFF"/>
        <SolidColorBrush x:Key="CardBorder" Color="#E6EAF0"/>
        <SolidColorBrush x:Key="Soft" Color="#F3F4F6"/>
        <SolidColorBrush x:Key="Accent" Color="#3B82F6"/>
        <SolidColorBrush x:Key="AccentDark" Color="#2563EB"/>
        <SolidColorBrush x:Key="Muted" Color="#64748B"/>

        <!-- حاوية -->
        <Style x:Key="CardBorderStyle" TargetType="Border">
            <Setter Property="Background" Value="{StaticResource CardBg}"/>
            <Setter Property="BorderBrush" Value="{StaticResource CardBorder}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="16"/>
            <Setter Property="Padding" Value="14"/>
        </Style>

        <!-- مدخلات -->
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#E2E8F0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,8"/>
        </Style>
        <Style TargetType="ComboBox">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#E2E8F0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,8"/>
        </Style>
        <Style TargetType="DatePicker">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#E2E8F0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,8"/>
        </Style>
        <Style TargetType="CheckBox">
            <Setter Property="Margin" Value="0,8,0,0"/>
        </Style>
        <Style TargetType="Button" x:Key="PrimaryBtn">
            <Setter Property="Background" Value="{StaticResource Accent}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="BorderBrush" Value="{StaticResource AccentDark}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>
    </Window.Resources>

    <!-- اختصارات -->
    <Window.InputBindings>
        <KeyBinding Key="S" Modifiers="Ctrl" Command="{Binding SaveCommand}"/>
        <KeyBinding Key="Escape" Command="{Binding CancelCommand}"/>
    </Window.InputBindings>

    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/> <!-- أزرار أعلى -->
            <RowDefinition Height="Auto"/> <!-- البيانات الرئيسية -->
            <RowDefinition Height="*"/>     <!-- حدود العملات -->
            <RowDefinition Height="Auto"/> <!-- تلميح سفلي -->
        </Grid.RowDefinitions>

        <!-- شريط أعلى -->
        <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
            <controls:Button Content="حفظ" Width="130" Style="{StaticResource PrimaryBtn}"
                    Command="{Binding SaveCommand}" Margin="0,0,10,0"/>
            <controls:Button Content="إلغاء" Width="110" Command="{Binding CancelCommand}"/>
        </StackPanel>

        <!-- البيانات الرئيسية -->
        <controls:Card Grid.Row="1" Padding="14" Margin="0,0,0,10">
            <Grid Margin="4">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- اسم الصندوق -->
                <StackPanel Grid.Column="0" Grid.Row="0" Margin="8">
                    <TextBlock Text="اسم الصندوق" Foreground="{StaticResource Muted}" FontSize="12" Margin="0,0,0,4"/>
                    <controls:TextBox Text="{Binding Model.FundName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                </StackPanel>

                <!-- حساب الصندوق (كاش) -->
                <StackPanel Grid.Column="1" Grid.Row="0" Margin="8">
                    <TextBlock Text="حساب الصندوق (كاش)" Foreground="{StaticResource Muted}" FontSize="12" Margin="0,0,0,4"/>
                    <ComboBox ItemsSource="{Binding AccountOptions}"
                              SelectedValue="{Binding Model.AccountNumber, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                              SelectedValuePath="AccountCode"
                              DisplayMemberPath="AccountDisplay"
                              IsEditable="True"
                              IsTextSearchEnabled="True"
                              StaysOpenOnEdit="True"/>
                    <TextBlock Foreground="Gray" FontSize="11" Margin="0,4,0,0"
                               Text="ابحث باسم/كود الحساب… يتم حفظ الكود فقط."/>
                </StackPanel>

                <!-- الشركة -->
                <StackPanel Grid.Column="0" Grid.Row="1" Margin="8">
                    <TextBlock Text="الشركة" Foreground="{StaticResource Muted}" FontSize="12" Margin="0,0,0,4"/>
                    <ComboBox ItemsSource="{Binding CompanyOptions}"
                              SelectedValue="{Binding SelectedCompanyId, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                              SelectedValuePath="CompId"
                              DisplayMemberPath="CompName"/>
                </StackPanel>

                <!-- الفرع -->
                <StackPanel Grid.Column="1" Grid.Row="1" Margin="8">
                    <TextBlock Text="الفرع" Foreground="{StaticResource Muted}" FontSize="12" Margin="0,0,0,4"/>
                    <ComboBox ItemsSource="{Binding BranchOptions}"
                              SelectedValue="{Binding SelectedBranchId, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                              SelectedValuePath="BranchId"
                              DisplayMemberPath="BranchName"/>
                </StackPanel>

                <!-- وظيفة الصندوق -->
                <StackPanel Grid.Column="0" Grid.Row="2" Margin="8">
                    <TextBlock Text="وظيفة الصندوق" Foreground="{StaticResource Muted}" FontSize="12" Margin="0,0,0,4"/>
                    <ComboBox ItemsSource="{Binding Source={StaticResource FundFunctionValues}}"
                              SelectedValue="{Binding Model.FundType, Mode=TwoWay}"
                              SelectedValuePath="."
                              MinWidth="180">
                        <ComboBox.ItemTemplate>
                            <DataTemplate>
                                <TextBlock>
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Text" Value="{Binding}" />
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding}" Value="{x:Static models:FundType.ReceiptOnly}">
                                                    <Setter Property="Text" Value="للْقبض فقط"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding}" Value="{x:Static models:FundType.PaymentOnly}">
                                                    <Setter Property="Text" Value="للصرف فقط"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding}" Value="{x:Static models:FundType.Both}">
                                                    <Setter Property="Text" Value="للْقبض والصرف"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                            </DataTemplate>
                        </ComboBox.ItemTemplate>
                    </ComboBox>
                </StackPanel>

                <!-- نشط ومستخدم -->
                <StackPanel Grid.Column="1" Grid.Row="2" Margin="8">
                    <CheckBox Content="نشط" 
                              IsChecked="{Binding Model.IsActive, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                    <CheckBox Content="مستخدم" 
                              IsChecked="{Binding Model.IsUsed, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                </StackPanel>

                <!-- تاريخ الإيقاف وسبب الإيقاف -->
                <StackPanel Grid.Column="0" Grid.Row="3" Margin="8">
                    <TextBlock Text="تاريخ الإيقاف" Foreground="{StaticResource Muted}" FontSize="12" Margin="0,0,0,4"/>
                    <DatePicker SelectedDate="{Binding Model.StopDate, Mode=TwoWay}"/>
                </StackPanel>

                <StackPanel Grid.Column="1" Grid.Row="3" Margin="8">
                    <TextBlock Text="سبب الإيقاف" Foreground="{StaticResource Muted}" FontSize="12" Margin="0,0,0,4"/>
                    <controls:TextBox Text="{Binding Model.StopReason, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                </StackPanel>
            </Grid>
        </controls:Card>

        <!-- حدود العملات -->
        <controls:Card Grid.Row="2" Padding="14">
            <DockPanel>
                <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,0,0,8">
                    <TextBlock Text="حدود العملات" FontWeight="SemiBold" Foreground="#111827" VerticalAlignment="Center" Margin="0,0,10,0"/>
                    <controls:Button Content="إضافة صف" Command="{Binding AddLimitRowCommand}" Padding="8,4"/>
                </StackPanel>

                <DataGrid ItemsSource="{Binding LimitRows}"
                          AutoGenerateColumns="False"
                          CanUserAddRows="False" 
                          CanUserDeleteRows="False" 
                          IsReadOnly="False">
                    <DataGrid.Columns>
                        <DataGridTemplateColumn Header="العملة" MinWidth="150">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <ComboBox ItemsSource="{Binding DataContext.CurrencyOptions, RelativeSource={RelativeSource AncestorType=Window}}"
                                              SelectedValuePath="CurrencyId"
                                              DisplayMemberPath="CurrencyNameAr"
                                              SelectedValue="{Binding CurrencyId, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <DataGridTextColumn Header="أدنى حد نقدي"
                                            Binding="{Binding MinCash, StringFormat=N2, UpdateSourceTrigger=PropertyChanged}" />
                        <DataGridTextColumn Header="أعلى حد نقدي"
                                            Binding="{Binding MaxCash, StringFormat=N2, UpdateSourceTrigger=PropertyChanged}" />
                        <DataGridTextColumn Header="أدنى حد للتسوية"
                                            Binding="{Binding MinSettlement, StringFormat=N2, UpdateSourceTrigger=PropertyChanged}" />
                        <DataGridTextColumn Header="أعلى حد للتسوية"
                                            Binding="{Binding MaxSettlement, StringFormat=N2, UpdateSourceTrigger=PropertyChanged}" />
                        <DataGridCheckBoxColumn Header="تجاوز الحدود" Binding="{Binding AllowLimitExceed}" />

                        <DataGridTemplateColumn Header="حذف" Width="80">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Button Content="✖"
                                            Command="{Binding DataContext.RemoveLimitRowCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                            CommandParameter="{Binding}"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </DockPanel>
        </controls:Card>

        <!-- شريط سفلي -->
        <TextBlock Grid.Row="3"
                   Text="💡 نصيحة: تأكد من ملء اسم الصندوق والحساب والشركة والفرع قبل الحفظ."
                   Foreground="{StaticResource Muted}" Margin="4,10,0,0"/>
    </Grid>
</Window>

