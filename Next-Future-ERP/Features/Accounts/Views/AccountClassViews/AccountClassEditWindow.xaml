<Window x:Class="Next_Future_ERP.Features.Accounts.Views.AccountClassEditWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        xmlns:controls="clr-namespace:Wpf.Ui.Controls;assembly=Wpf.Ui"
        Title="{Binding WindowTitle}" Height="500" Width="800"
        WindowStartupLocation="CenterOwner"
        FlowDirection="RightToLeft"
        Background="#F7F9FC"
        ResizeMode="CanResizeWithGrip"
        MinWidth="700" MinHeight="400"
        Loaded="Window_Loaded">

    <!-- اختصارات -->
    <Window.InputBindings>
        <KeyBinding Key="S" Modifiers="Ctrl" Command="{Binding SaveCommand}"/>
        <KeyBinding Key="Escape" Command="{Binding CancelCommand}"/>
    </Window.InputBindings>

    <Window.Resources>
        <!-- ألوان -->
        <SolidColorBrush x:Key="CardBg" Color="#FFFFFFFF"/>
        <SolidColorBrush x:Key="CardBorder" Color="#E6EAF0"/>
        <SolidColorBrush x:Key="Soft" Color="#F3F4F6"/>
        <SolidColorBrush x:Key="Accent" Color="#3B82F6"/>
        <SolidColorBrush x:Key="AccentDark" Color="#2563EB"/>
        <SolidColorBrush x:Key="Muted" Color="#64748B"/>

        <!-- حاوية -->
        <Style x:Key="CardBorderStyle" TargetType="Border">
            <Setter Property="Background" Value="{StaticResource CardBg}"/>
            <Setter Property="BorderBrush" Value="{StaticResource CardBorder}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="16"/>
            <Setter Property="Padding" Value="14"/>
        </Style>

        <!-- مدخلات -->
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#E2E8F0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,8"/>
        </Style>
        <Style TargetType="ComboBox">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#E2E8F0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="6,4"/>
        </Style>
        <Style TargetType="Button" x:Key="PrimaryBtn">
            <Setter Property="Background" Value="{StaticResource Accent}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="BorderBrush" Value="{StaticResource AccentDark}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>
    </Window.Resources>

    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/> <!-- أزرار أعلى -->
            <RowDefinition Height="*"/>    <!-- المحتوى -->
            <RowDefinition Height="Auto"/> <!-- تلميح سفلي -->
        </Grid.RowDefinitions>

        <!-- شريط أعلى -->
        <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
            <controls:Button Content="حفظ" Width="130" Style="{StaticResource PrimaryBtn}"
                    Command="{Binding SaveCommand}" Margin="0,0,10,0"/>
            <controls:Button Content="إلغاء" Width="110" Command="{Binding CancelCommand}"/>
        </StackPanel>

        <!-- المحتوى -->
        <controls:Card Grid.Row="1" Padding="14">
            <Grid Margin="4">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="1.2*"/>
                    <ColumnDefinition Width="1.2*"/>
                    <ColumnDefinition Width="1.6*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- الاسم العربي -->
                <StackPanel Grid.Column="0" Margin="8">
                    <TextBlock Text="الاسم العربي" Foreground="{StaticResource Muted}" FontSize="12"/>
                    <controls:TextBox Text="{Binding Model.AccountClassAname, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                </StackPanel>

                <!-- الاسم الإنجليزي -->
                <StackPanel Grid.Column="1" Margin="8">
                    <TextBlock Text="الاسم الإنجليزي" Foreground="{StaticResource Muted}" FontSize="12"/>
                    <controls:TextBox Text="{Binding Model.AccountClassEname, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                </StackPanel>

                <!-- التصنيف -->
                <StackPanel Grid.Column="2" Margin="8">
                    <TextBlock Text="التصنيف" Foreground="{StaticResource Muted}" FontSize="12"/>
                    <ComboBox ItemsSource="{Binding CategoryOptions}"
                              SelectedItem="{Binding SelectedCategoryOption}"
                              DisplayMemberPath="CategoryNameAr"
                              IsEditable="True"
                              Text="{Binding CategorySearchText, UpdateSourceTrigger=PropertyChanged}"/>
                </StackPanel>

                <!-- معلومات إضافية -->
                <controls:Card Grid.ColumnSpan="3" Grid.Row="1" Margin="8,16,8,0" Padding="12">
                    <StackPanel>
                        <TextBlock Text="معلومات إضافية" FontWeight="SemiBold" Foreground="#111827" Margin="0,0,0,8"/>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <TextBlock Grid.Column="0" Text="معرف التصنيف:" Foreground="#6B7280" Margin="0,0,10,0"/>
                            <TextBlock Grid.Column="1" Text="{Binding Model.AccountClassId}" 
                                       FontWeight="SemiBold" Foreground="#111827"/>
                        </Grid>
                    </StackPanel>
                </controls:Card>
            </Grid>
        </controls:Card>

        <!-- شريط سفلي -->
        <TextBlock Grid.Row="2"
                   Text="💡 نصيحة: تأكد من ملء جميع الحقول المطلوبة قبل الحفظ."
                   Foreground="{StaticResource Muted}" Margin="4,10,0,0"/>
    </Grid>
</Window>
