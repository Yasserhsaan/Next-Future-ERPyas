<UserControl x:Class="Next_Future_ERP.Features.InitialSystem.Views.Pages.CompanyInfoPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:vm="clr-namespace:Next_Future_ERP.Features.InitialSystem.ViewModels"
             mc:Ignorable="d"
             FlowDirection="RightToLeft"
             d:DesignHeight="900" d:DesignWidth="500">

    <!--<UserControl.DataContext>
        <vm:CompanyInfoViewModel />
    </UserControl.DataContext>-->

    <Grid>

        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <StackPanel >
                <!-- Title -->
                <StackPanel>
                    <ui:TextBlock Text="معلومات الشركة" FontSize="20" FontWeight="Bold" TextAlignment="Center" />
                </StackPanel>

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />

                    </Grid.ColumnDefinitions>
                    <!-- 🏢 Company Info -->
                    <GroupBox Header="بيانات الشركة" FontWeight="SemiBold">
                        <StackPanel  Margin="12" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
                            <ui:TextBlock Text="اسم الشركة:" />
                            <ui:TextBox Text="{Binding CompanyName}" PlaceholderText="ادخل اسم الشركة" />


                            <StackPanel Orientation="Horizontal">
                                <StackPanel  Grid.Column="0" Width="360">

                                    <ui:TextBlock Text="شعار الشركة:" />
                                    <ui:Button Content="اختيار الشعار" VerticalAlignment="Stretch" HorizontalAlignment="Stretch" Command="{Binding ChooseImageCommand}" />
                                    <ui:TextBlock Text="اللغة:" />
                                    <ComboBox ItemsSource="{Binding Languages}" SelectedItem="{Binding SelectedLanguage}" />
                                </StackPanel>

                                <Border Width="130" Height="90" BorderBrush="#ccc" BorderThickness="1" Margin="18">
                                    <Image Source="{Binding LogoImage}" Stretch="Uniform" HorizontalAlignment="Center"/>
                                </Border>
                            </StackPanel>
                            <ui:TextBlock Text="العملة:" />
                            <ComboBox ItemsSource="{Binding Currencies}" SelectedItem="{Binding SelectedCurrency}" />

                        </StackPanel>

                    </GroupBox>
                </Grid>
                <!-- 🌐 Localization + ⚙ Options Side-by-Side -->
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <!-- 🔢 Accounting Settings -->
                    <GroupBox Header="إعدادات الحسابات"  Margin="0,0,10,0"  Grid.Column="0">
                        <StackPanel  Margin="12">
                            <ui:TextBlock Text="السنة المالية:" />
                            <ComboBox ItemsSource="{Binding FiscalYears}" SelectedItem="{Binding FiscalYear}" />
                            <ui:TextBlock Text="الحد الأدنى لطول كلمة المرور:" />
                            <ui:TextBox Text="{Binding MinPasswordLength}" />

                            <ui:TextBlock Text="طول رقم الحساب:" />
                            <ui:TextBox Text="{Binding AccountNumberLength}" />

                            <ui:TextBlock Text="مستوى الحسابات الفرعية:" />
                            <ComboBox ItemsSource="{Binding SubAccountLevels}" SelectedItem="{Binding SubAccountLevel}" />
                        </StackPanel>
                    </GroupBox>


                    <StackPanel
                         Grid.Column="1"
                            
                        
                        >
                        <!-- ⚙️ System Options -->
                        <GroupBox Header="الخيارات العامة"  Height="275" >
                            <StackPanel Margin="12">
                                <CheckBox Content="تفعيل التقويم الهجري" IsChecked="{Binding EnableHijriCalendar}" />
                                <CheckBox Content="تمكين العملات الأجنبية" IsChecked="{Binding EnableForeignCurrency}" />
                                <CheckBox Content="تمكين دخول من عدة أجهزة" IsChecked="{Binding EnableMultiDeviceLogin}" />
                                <CheckBox Content="تفعيل ضريبة القيمة المضافة" IsChecked="{Binding UseVat}" />
                                <CheckBox Content="تفعيل مراكز التكلفة" IsChecked="{Binding EnableCostCenters}" />
                            </StackPanel>
                        </GroupBox>
                    </StackPanel>

                </Grid>


                <Grid>
                    <GroupBox Header="إعدادات ترقيم الحسابات" Margin="10">
                        <Grid Margin="12">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>

                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>

                            <!-- Column 0 -->
                            <StackPanel Grid.Column="0" Grid.Row="0" Margin="5">
                                <ui:TextBlock Text="بادئه الاصول:" />
                                <ComboBox ItemsSource="{Binding ListAccountNumbers}" 
                                          SelectedItem="{Binding SelectedAssetsStart}" 
                                          DisplayMemberPath="Value"
                                          
                                          />
                            </StackPanel>

                            <StackPanel Grid.Column="0" Grid.Row="1" Margin="5">
                                <ui:TextBlock Text="بادئه الايرادات:" />
                                <ComboBox ItemsSource="{Binding ListAccountNumbers}" 
                                          SelectedItem="{Binding SelectedRevenueStart}"  DisplayMemberPath="Value"/>
                            </StackPanel>

                            <!-- Column 1 -->
                            <StackPanel Grid.Column="1" Grid.Row="0" Margin="5">
                                <ui:TextBlock Text="بادئه الالتزامات:" />
                                <ComboBox ItemsSource="{Binding ListAccountNumbers}" SelectedItem="{Binding SelectedLiabilitiesStart}" DisplayMemberPath="Value" />
                            </StackPanel>

                            <StackPanel Grid.Column="1" Grid.Row="1" Margin="5">
                                <ui:TextBlock Text="بادئه المصروفات:" />
                                <ComboBox ItemsSource="{Binding ListAccountNumbers}" SelectedItem="{Binding SelectedExpenseStart}"  DisplayMemberPath="Value"/>
                            </StackPanel>

                            <!-- Full width ComboBox -->
                            <StackPanel Grid.Column="0" Grid.Row="2" Grid.ColumnSpan="2" Margin="5">
                                <ui:TextBlock Text="بادئه حقوق الملكية:" />
                                <ComboBox ItemsSource="{Binding ListAccountNumbers}" SelectedItem="{Binding SelectedEquityStart}" DisplayMemberPath="Value" />
                            </StackPanel>
                        </Grid>
                    </GroupBox>
                </Grid>




            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
