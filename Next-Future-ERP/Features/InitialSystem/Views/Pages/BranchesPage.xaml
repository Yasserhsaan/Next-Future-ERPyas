<UserControl x:Class="Next_Future_ERP.Features.InitialSystem.Views.Pages.BranchesPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:vm="clr-namespace:Next_Future_ERP.Features.InitialSystem.ViewModels"
             mc:Ignorable="d"
             FlowDirection="RightToLeft"
             d:DesignHeight="900" d:DesignWidth="500">  


    <Grid >
       

       

        <!-- Right Panel -->
        <Border Background="{Binding ControlBackgroundBrush}" CornerRadius="8" Padding="24">
            <StackPanel>

                <!-- Step Title -->
                <ui:TextBlock Text="بيانات الفرع" 
                       FontSize="20" 
                       FontWeight="Bold" 
                       TextAlignment="Center"
                       Margin="0,0,0,10"/>


                <StackPanel>
                    <ui:TextBlock Text="اسم الفرع" />
                    <ui:TextBox Text="{Binding BranchName}" />
                </StackPanel>

                <!-- Current Month -->
                <StackPanel>
                    <ui:TextBlock Text="موقع الفرع" />
                    <ui:TextBox Text="{Binding Location}" />
                </StackPanel>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" Margin="0,10,0,0">
                    
                    <CheckBox IsChecked="{Binding IsActive}"/>
                    <ui:TextBlock Text="تقعيل" VerticalAlignment="Center"/>
                </StackPanel>

            </StackPanel>
        </Border>
    </Grid>
</UserControl>
