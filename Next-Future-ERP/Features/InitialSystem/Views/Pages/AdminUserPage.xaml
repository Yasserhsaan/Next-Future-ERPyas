<UserControl x:Class="Next_Future_ERP.Features.InitialSystem.Views.Pages.AdminUserPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:Next_Future_ERP.Features.InitialSystem.ViewModels"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="500"
             FlowDirection="RightToLeft">

    

    <Border Padding="20" CornerRadius="10" Background="{DynamicResource ControlBackgroundBrush}">
        <StackPanel>
            <!-- Header -->
            <StackPanel Margin="0,0,0,20" HorizontalAlignment="Center">
               
                <ui:TextBlock Text="إنشاء مستخدم مسؤول" FontWeight="Bold" FontSize="20" HorizontalAlignment="Center" />
            </StackPanel>

            <!-- Input Form -->
            <Grid Margin="0,0,0,20">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="120" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <!-- Code -->
                <ui:TextBlock Grid.Row="0" Grid.Column="0" Text="الرمز" VerticalAlignment="Center" />
                <ui:TextBox Grid.Row="0" Grid.Column="1" Text="{Binding Code}" Margin="5" />

                <!-- Name -->
                <ui:TextBlock Grid.Row="1" Grid.Column="0" Text="الاسم" VerticalAlignment="Center" />
                <ui:TextBox Grid.Row="1" Grid.Column="1" Text="{Binding Name}" Margin="5" />

                <!-- First Name -->
                <ui:TextBlock Grid.Row="2" Grid.Column="0" Text="الاسم الأول" VerticalAlignment="Center" />
                <ui:TextBox Grid.Row="2" Grid.Column="1" Text="{Binding FirstName}" Margin="5" />

                <!-- Mobile -->
                <ui:TextBlock Grid.Row="3" Grid.Column="0" Text="الجوال" VerticalAlignment="Center" />
                <ui:TextBox Grid.Row="3" Grid.Column="1" Text="{Binding Mobile}" Margin="5" />

                <!-- Phone -->
                <ui:TextBlock Grid.Row="4" Grid.Column="0" Text="الهاتف" VerticalAlignment="Center" />
                <ui:TextBox Grid.Row="4" Grid.Column="1" Text="{Binding Phone}" Margin="5" />

                <!-- Address -->
                <ui:TextBlock Grid.Row="5" Grid.Column="0" Text="العنوان" VerticalAlignment="Center" />
                <ui:TextBox Grid.Row="5" Grid.Column="1" Text="{Binding Address}" Margin="5" />

                <!-- Email -->
                <ui:TextBlock Grid.Row="6" Grid.Column="0" Text="البريد الإلكتروني" VerticalAlignment="Center" />
                <ui:TextBox Grid.Row="6" Grid.Column="1" Text="{Binding Email}" Margin="5" />

                <!-- Password -->
                <ui:TextBlock Grid.Row="7" Grid.Column="0" Text="كلمة المرور" VerticalAlignment="Center" />
               
                <ui:PasswordBox Grid.Row="7" Grid.Column="1"  Margin="5"   IsEnabled="{Binding Password}"   
        PlaceholderEnabled="True"  
    Password="{Binding Password, Mode=TwoWay}"
    x:Name="Password"
        PlaceholderText="كلمة السر..." />
            </Grid>
            

          
        </StackPanel>
    </Border>
</UserControl>
